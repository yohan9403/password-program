# implement dictionary (hash_table), so that you can populate it with password file in the beginning of enroll and authnticate program.

# then you can check for id or password verification a lot faster.
